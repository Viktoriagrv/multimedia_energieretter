// Zeigt den Text und den Link nach 4 Sekunden
setTimeout(function() {
    // Link wird nach 4 Sekunden angezeigt und bleibt sichtbar
    document.getElementById("button-container").style.display = "block"; 
}, 6000);  // Link erscheint nach 4 Sekunden
